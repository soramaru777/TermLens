# TermLens Wiki — Operation Log

追記のみ。新しいものを下に足す。

---

2026-08-13 init — LLM Wiki を導入。`docs/raw/` と `docs/wiki/` を作成し、`~/wiki/mounts/termlens` から symlink で接続。
2026-08-13 ingest — `README.md` と `docs/local/status-2026-08-13.md` を取り込み、5ページを生成（architecture / stt-pipeline / term-extraction / deployment / open-issues）。横断知見2件を `~/wiki/knowledge/` へ昇格。
2026-08-13 ingest — Fly.io デプロイと iPad 実機テストのセッションを `docs/raw/session-2026-08-13-fly-deploy.md` として固定し取り込み。deployment（手順を `fly launch` → `apps create` に訂正、トライアル5分制限・`.dockerignore` 必須・HA 2台を追記）、open-issues（弱点12 バッファ肥大を新規、実機テストを一部解決、優先順位を再編）、stt-pipeline（diarization のウォームアップ挙動）、term-extraction（失敗時の再バッファ挙動）の4ページを更新。Fly.io の汎用知見を `~/wiki/knowledge/fly-io-deployment.md` へ昇格。
2026-08-14 update — CI/CD 導入に伴い deployment を改訂。develop への push で自動デプロイする構成、CI の認証（アプリスコープ限定トークン）、アクションを SHA 固定する方針を追記。手動デプロイは初回構築時の手順として残した。あわせて credential helper の評価順による push 404 の対処を追加。
2026-08-15 update — 公開前の棚卸し。open-issues から運用10（認証情報の取り扱いに関する記述）を削除。該当の資格情報はローテーション済みで課題が解消したため。あわせて運用11にレート制限の欠如を追記。
2026-08-16 update — ライセンスを MIT に決定（依存88パッケージがすべて permissive でコピーレフト無しを確認）。レート制限は「トークンが128bitのため不要」と判断し、open-issues の運用11から除外して deployment に判断の根拠を記録。AUTH_TOKEN の強度要件（暗号論的乱数で128bit以上、入力性から16進数を推奨）と、URL が CT ログにより秘匿できないことを deployment に追記。
2026-08-17 update — 会議ログの Markdown エクスポートを実装（弱点5 を解決）。open-issues の優先順位を再編し、実装上の判断（cardData Map の追加、経過時間が受信時刻ベースであること、PWA での共有シート優先が未検証であること）を記録。
2026-08-17 update — 狭い画面で用語カードを最新1枚だけ表示する挙動を追加（追従／固定／最新の3状態）。閾値 899px を app.js と style.css で共用している点、iPad 縦向きも1枚表示になる点を open-issues に記録。
2026-08-17 update — PR #7 のレビュー指摘に対応。セッション終端でのマイク/AudioContext/Wake Lock 解放を finish() に集約し、未保存警告を文字起こし/用語カードで分離。検証中に見つかった「停止ボタンの二重ハンドラで戻るが機能しない」不具合もあわせて修正し、いずれも open-issues に注意点として記録。
2026-08-17 update — PR #7 の2巡目レビュー(5件)に対応。セッション再開時の状態初期化、1006 判定のガード、破棄警告フラグのリセット、解放処理での await 前のグローバル退避、Wake Lock 取得競合、<a download> の成否判定を修正し、いずれも再発防止の注意点として open-issues に追記。
2026-08-18 update — LLM を Anthropic Claude Sonnet 5 から OpenAI GPT-5.6 Luna へ移行。ベンチで抽出・清書とも品質が同等（誤認識復元 11/11）、レイテンシ 3割〜3倍速、コスト 7〜21倍安を確認したうえでの判断。あわせて enrich.ts に引用除去・URL正規化・字数ガードを追加（モデル非依存の既存不具合）。残高切れが 400 から 429 に変わるため、scheduler の恒久エラー判定に insufficient_quota の判別を追加。
2026-08-18 update — Issue #9 前半（自動再接続）を実装。WS の意図しない切断で即 `finish()` せず、指数バックオフ（1s/2s/4s/8s/16s、最大5回）で `connectWs()` を再実行するようにした。マイクは再接続中も離さず送信だけ止める、認証失敗判定と停止操作中は再接続しない、停止時は待機タイマーを `clearTimeout` する、再接続成功時は話者番号の振り直しを示す区切り(`{ type: "reconnect" }`)を文字起こしとエクスポート双方に挿入、という判断を open-issues に記録。セッション復元（後半）は未着手のまま残した。
2026-08-19 update — Issue #9 後半（セッション復元）を実装。`finalLines`・`cardData`・`sessionStartedAt` を `localStorage`（`termlens.session`）に1秒デバウンスで保存し(`visibilitychange` の `hidden` で即時フラッシュ)、ホーム画面起動時に24時間以内の保存があれば日時・発言数・カード数を示す案内から復元できるようにした。復元は本番画面を終端状態で開くだけで `connectWs()` は呼ばない(録音再開なし)。設定に保存ON/OFFのチェックボックス(`termlens.persist`、既定ON)を追加し、OFF時は既存データも削除。正常に「戻る」で帰るときも削除する。トークン・音声は保存対象外。容量超過対策として2MB超で古い `finalLines` から間引き、書き込みは try/catch で保護。open-issues の弱点4を解決済みに更新。iOS 実機でのタブ破棄からの復元検証は未実施のまま残る。
2026-08-19 update — #8/#9/#10/#14 のレビュー指摘13件に対応。(1) 再接続時にクライアントが `cardData` の term 一覧を `start` メッセージの `shownTerms` として送るようにし(`src/protocol.ts`/`src/session.ts`/`src/extract/scheduler.ts`)、サーバー側デデュープを空から始めない形にしてカード二重化とドラフト上書きを解消。あわせて `addCard()` を冪等化し、清書済み(links あり)カードはドラフトで上書きしない。(2) カードのリンク描画を `renderCardLinks()` に共通化し、`addCard()` でも links があれば即描画するようにして、復元直後の「確認中」固定を解消。(3) `connectWs()` を `sock`(ローカル変数)＋`if (sock !== ws) return` の型に書き換え、停止操作中の CONNECTING ソケットに対する古いイベントを無視するようにした。`releaseCapture()` は `ws=null` の前に `ws?.close()` を呼ぶ。(4) `finish()` で `releaseCapture()` の前に保留中のデバウンス保存を強制フラッシュ。(5) `ServerMessage.error` に `permanent?: boolean` を追加し、scheduler の恒久エラー打ち切り時だけ true にした。クライアントは `permanent` が真の時だけバナーを出す(一時エラー・`stt_error` はステータス表示のみ)。(6) 「開始」時に未復元の保存セッションがあれば明示的に破棄し、復元バナーにも「開始すると前回の内容は破棄される」旨を追記。(7) `buildTermsMarkdown()` のリンク出力に `isHttpUrl` 検証を追加。(8) 設定保存でOFFにした際 `pendingRestoreSession`/バナーも戻すようにし、`checkPendingSession()` も `getPersistEnabled()` を見るようにした。(9) 発言・カードともに空のセッションは保存/案内表示のどちらもしないようにした。(10) 復元データの `finalLines`/`cardData` に `Array.isArray` 検証を追加し、復元処理全体を try/catch で保護、失敗時は `showError()` で通知してホームに留まる。(11) `SESSION_MAX_CHARS` を 2M→1M 文字に縮小し、`setItem` 失敗時に直近50件まで削って1回だけ再試行する。(12) 復元完了時にも `finishing = true` を立てて、将来 `finish()` を通る経路が増えても多重実行ガードが効くようにした。(13) 本ページの1006判定の記述を実装(`!everReady && 1006`)に合わせて更新し、「無音での誤表示」を避けるための変更である旨を明記。
2026-08-19 update — PR #15 のレビュー指摘に対応。start.shownTerms の要素型を検証しないとサーバーが落ちる問題を修正し、WS 入力の検証方針を open-issues に追記。
2026-08-19 update — PR #15 の2巡目レビューに対応。復元失敗時にセッションの内容が残り、次の会議の shownTerms に混ざって汚染する問題を修正（clearSessionContent を resetSessionState と復元の catch から通す）。
2026-08-19 update — TOPページの初回表示が遅い件を実測（起動中0.03秒 / stop からの起動7.2秒 / suspend からの復帰0.39秒）。auto_stop_machines を "suspend" に変更し、比較表と注意点を deployment に記録。
2026-08-20 update — 文字起こし精度の改善。ダウンサンプリングにアンチエイリアスFIRが無く8kHz超が0dBで折り返していた件を修正（実測 -65dB以下）、endpointing=300 で文の途中の分断を抑制、対面会議向けにエコーキャンセル/ノイズ抑制を無効化。stt-pipeline に記録。
2026-08-25 update — Issue #18「匿名化 Golden Test の整備」。評価基盤を2層に分けて導入した。決定的層（`npm test` / CI 常時）は `node:test` + `tsx --test` で純関数を固定する（`normalizeTerm`・`isPermanent`/`toUserMessage`・`stripInlineCitations`・`dominantSpeaker`・`designLowpass`・評価指標の計算）。LLM を呼ぶ層は `RUN_LLM_EVAL=1` のときだけ走るオプトインにし、既定の `npm test` からは skip する。新規ページ [[termlens-testing]] を作成し、index に追加。あわせて (1) `tsconfig.json` は触らず型チェック専用の `tsconfig.test.json` を新設（tests/ を include に足すと rootDir が繰り上がって `dist/src/server.js` になり `npm start` が壊れるため）、(2) `designLowpass` を `public/lowpass.js` に切り出して Node から検証できるようにし、(3) ローパスの阻止域を実測して 48kHz で -64.6dB（-65dB にわずかに届かない）と判明したため閾値を -64.0dB に確定、stt-pipeline の記述を訂正した。open-issues には「このテスト基盤で測れるようになったこと／CER・WER と Deepgram の話者クラスタリング精度は依然として測れないこと」を記録。
2026-08-25 update — Issue #18 のレビュー指摘15件に対応。核は「テストが守るべきものを実際に守れているか」。(1) ローパスの設計パラメータ（`FIR_TAPS`/`CUTOFF_MARGIN`/`TARGET_SAMPLE_RATE`）を `public/lowpass.js` に集約し、`audio-processor.js`・`app.js`・`tests/lowpass.test.ts` がそこから import するようにした（テストが値をコピーしていて drift を検出できなかった）。阻止域のコメントも実測値に訂正。(2) `normalizeTerm` を依存ゼロの `src/extract/normalize.ts` に移動し、OpenAI クライアントを抱える `scheduler.ts` との同居を解消。残りのダミーキー注入は `tsx --test --import ./tests/helpers/openai-env.ts` でランナーレベルに寄せ、import 順依存を無くした。ヘルパはキーの有無で分岐せず無条件に上書きする（`RUN_LLM_EVAL=1` のときだけ例外）。(3) 評価ハーネスの false PASS を塞いだ: `EVAL_RUNS`/`EVAL_CONCURRENCY` を1以上の整数として検証、ジョブ0件は例外、1ジョブの失敗は `jobErrors` に記録して集計は続行しつつ失敗が1件でもあれば PASS にしない。レポートの `model` は `config.llmModel` を記録する。`OPENAI_API_KEY` 未設定は読める1行で落ちるよう `createExtractor` を dynamic import に変更。(4) Precision を正規化キーで dedupe してから集計（Recall 側だけ dedupe されていた）。(5) `tsconfig.json` の `exclude` に `src/eval` を追加し、`dist/eval/` が本番イメージに入らないようにした（`tsconfig.test.json` は `"exclude": []` で型チェック対象に戻す）。(6) 96kHz 入力では阻止域が -21.9dB まで劣化することを実測値として stt-pipeline に記録（既存挙動、テストは追加せず）。
2026-08-25 update — Issue #19「STT の word 情報・confidence・speaker 情報を保持する」。案A（サーバー内部だけで保持）で実装した。(1) `TranscriptWord` を `src/stt/types.ts` に定義し、`TranscriptEvent.words?` を **optional** で追加（必須にすると `SttAdapter` の既存実装が即座に型エラーになり「既存を壊さない」AC と衝突するため）。(2) `src/stt/deepgram.ts` に `toTranscriptWords()` を追加して export。`punctuated_word` → `punctuatedWord` のキャメルケース化だけ行い、words が無ければ空配列ではなく `undefined` を返す。`dominantSpeaker()` の呼び出しとロジックは一切変更していない（置き換えは後続 Issue）。(3) **`src/protocol.ts` / `src/session.ts` / `public/app.js` は無変更**。words をクライアントへ送ると WS ペイロードが約13倍になり、`SESSION_MAX_CHARS` の間引きで `localStorage` 復元が静かに劣化するため、`session.ts` まで運んで止める。用途（word 単位話者分割・confidence 補正）はどちらもサーバー側で完結する。(4) mock も同じ型を出すようにした。日本語は分かち書きしないので `MockLine.words` は手書き（12行）で、実物の Deepgram の区切りとは「形が同じだけの別物」であることを stt-pipeline に明記。`start`/`end` は 8 文字/秒から機械算出し、confidence は既定 0.95・誤認識を模した語だけ 0.55 とした。低 confidence にする語は `tests/fixtures/term-cases.json` の `expectCorrection` のキー（評価ハーネスが「補正されるべき」と定義済みの表記）に揃え、`エンベディング` のような通常のカタカナ語は含めていない。interim は境界に跨る word を含めず、`text` も word 境界で切って `words.join("") === text` を全段で成立させる。(5) テストを2本追加（`tests/transcript-words.test.ts` / `tests/mock-words.test.ts`）。いずれも LLM を呼ばない決定的層。mock は `node:test` の偽タイマーで実際に発行されるイベントを検証している。fixture は #18 の匿名化方針を継続し、`word` はダミー文字列。`diarize-words.json` の「`speaker` 以外のキーが無い」検査を緩めないよう別ファイル（`deepgram-words.json`）にした。
2026-08-25 update — Issue #19 のレビュー指摘11件に対応。核は「mock を実 Deepgram と同じセマンティクスにする」。(1) 句読点を独立 word にするのをやめ、`MockLine.words` を `{ word, punctuated? }` の組にした（実 Deepgram は句読点を `punctuated_word` に付随させる。独立 word にすると 1 行あたりの word 数が1割水増しされ、word 数ベースの閾値が mock と実機でズレる。副次的に「`punctuatedWord` が常に `word` と同じで情報量ゼロ」も解消）。不変条件は `words.map(w => w.punctuated ?? w.word).join("") === text` に移した。(2) `start`/`end` が行ごとに 0 へ戻っていた（`TranscriptWord` の「ストリーム先頭からの相対」という契約と矛盾）のを、`MockSttAdapter.streamOffsetSec` を行またぎで持ち越すよう修正。行間ギャップは `MOCK_LINE_GAP_SEC = 1.5`（final 後に実際に待つ 1500ms と一致）。スクリプト一周を通した単調増加をテストで固定。(3) mock も空 interim を送らないようガード（`deepgram.ts` の `if (text.length > 0)` と同じ扱い）。あわせて全行の1段目 interim が空でないことを `buildMockWords` レベルで固定した。(4) `deepgram-words.json` の匿名性テストを許可キーのホワイトリスト＋`punctuated_word` の形式検査まで強化し（既存 `dominant-speaker.test.ts` と同水準）、期待値キー名を `expectSpeaker` → `expect` に統一。(5) `MOCK_MISHEARD_WORDS` を `term-cases.json` の `expectCorrection` のキー ∩ `MOCK_SCRIPT` の word と**集合一致**で検証（片方向だけだと term-cases に誤認識ケースを足したとき漏れを検出できない）。コメントの例示も `term-cases.json` に存在しない `カナリアデプロイ` から `スロットリング`（用語だが誤認識ではない対比例）に差し替え。(6) mock の再生テストの所要時間を固定値 60 秒から `MOCK_SCRIPT.length * MOCK_LINE_CYCLE_MS` の算出に変え、`finals.length >= MOCK_SCRIPT.length` で一周を固定。3テストで重複していた再生を1回に集約。(7) stt-pipeline の「約13倍」を実測ではなく試算であることが分かる書き方に訂正（`confidence: low` 相当）。`sliceMockWords` が `end` から逆算せず数え直している理由（roundSec の丸め誤差で境界比較がぶれる）をコメントで補った。

2026-08-26 update — Issue #20「word 単位で話者を分割し dominantSpeaker 依存をなくす」。案A（閾値なしの単純分割）で実装した。(1) `dominantSpeaker()` を削除し、分割ロジックを **`src/stt/split.ts` に依存ゼロの新モジュールとして**置いた（`splitBySpeaker()` / `buildFinalEvents()`）。`deepgram.ts` に置くと、使いたいだけの `mock.ts` とそのテストが `ws` と `config.ts` を連れ込み、`STT_PROVIDER=deepgram` かつキー未設定の環境で **mock のテストが import 時に throw する**（レビューで実際に再現した）。`src/extract/normalize.ts` を切り出したのと同じ判断。Deepgram 固有の `buildTranscriptEvents()` / `DeepgramAlternative` だけ `deepgram.ts` に残した。(2) 分割規則は「セグメントの speaker はその中で最初に現れた定義済み speaker」「定義済み speaker が現セグメントと異なったら新セグメント」「`undefined` は境界を作らず直前に吸収」の3点。(3) **最小語数の閾値は入れていない**。「短い相槌を別話者として残す」と「1語の話者番号の揺れを無視する」は同じ現象の裏表で、閾値を入れると 1〜2 語の相槌そのものが消え AC と衝突する。代償として `[0,1,0,1]` のような揺れは 4 分割として露出する。要否は #18 の評価基盤で測ってから判断する。(4) **interim は分割しない**。interim 表示ハンドラが上書きなので 2 件送ると前半が消える。interim の `speaker` はクライアントで読まれていないため `undefined` にした。(5) **text は組み立て直さず `transcript` から切り出す**。当初は `punctuatedWord ?? word` の連結にしていたが、**日本語の transcript でもラテン文字列の語間には空白が入る**（`AWS Lambda`）ためレビューで指摘され、`AWSLambda` に潰れると表示だけでなく `normalizeTerm()` のキーやハイライトの照合まで壊れると分かった。word の表記を `transcript` 上で順に探して境界で `slice` する方式に変え、見つからないときだけ連結にフォールバックする。単一話者セグメントは従来どおり完全な素通し。(6) **`src/stt/types.ts` / `src/protocol.ts` / `src/session.ts` / `public/app.js` / `src/stt/mock-script.ts` は無変更**。`onTranscript` は 1 メッセージにつき何回でも呼べるので、複数発話は `transcriptCb` を複数回呼ぶだけで型もプロトコルも変わらない。(7) ただし**イベント数が増えること自体の副作用**が3つあると分かり、[[termlens-stt-pipeline]] に節を追加した — 抽出バッファに `" "` 区切りが入る、`maybeRun()` の 20 秒経路で断片だけで LLM が走りうる（設計時の「LLM 呼び出し回数は変わらない」は**この経路について誤り**だった）、`finalLines` の件数増で `localStorage` の履歴が早く痩せる。いずれも実機で測る前に閾値で塞がない方針。(8) テストは `tests/split-by-speaker.test.ts`（config 非依存）と `tests/transcript-events.test.ts` に分け、`tests/dominant-speaker.test.ts` と `tests/fixtures/diarize-words.json` を削除。新 fixture `speaker-segments.json` は **id をケバブケース ASCII に限定し、日本語の説明はテスト側の定数に置いた**（fixture に自由記述の欄を作ると匿名化検査を当てられない穴になるため）。ホワイトリストは `expect` の入れ子にも当て、**ケース id の集合一致**で fixture の消し忘れ・足し忘れを検出する（`MOCK_MISHEARD_WORDS` で踏んだ片方向 drift の教訓）。(9) 未検証は増えた側にある — 多数決が吸収していた揺れが露出するため、細切れの度合いは複数話者の実声でしか測れない。[[termlens-open-issues]] の未検証1に追記した。

2026-08-26 update — Issue #21「speech_final / UtteranceEnd で発話単位を構築する」。案B（発話単位の構築＋抽出スケジューラが発話境界を尊重）で実装した。(1) **設計時に「スケジューラに境界判定を持ち込む」つもりだったが、詳細化すると不要だと分かった**。`scheduler.addFinal()` → `addUtterance()` と渡す単位を発話に変えるだけで、バッファの中身が「完成した発話の集まり」になり、切れ目が発話の途中に来なくなる。`maybeRun()` の発火条件（120字 / 10秒）は変えていない。これで懸念していた二重バッファリングの相互作用を持ち込まずに済んだ。(2) `src/stt/utterance.ts` を **依存ゼロ**で新設（`split.ts` と同じ理由）。確定契機は「`speechFinal`」「`UtteranceEnd`」「3秒タイムアウト」「500字上限」の4つ＋話者交代。(3) **`speech_final` と `UtteranceEnd` の併用は省略できない**。公式ドキュメントが「背景ノイズで VAD が反応し続けると `speech_final` が来ない」と明記しており、会議室の用途はまさにこれに当たる。`utterance_end_ms=1000` を追加した（`interim_results=true` は既に有効）。(4) **`speechFinal` は分割後の最後の1件にだけ立てる**。全件に立てると #20 の話者分割ごとに発話が閉じ、分割と結合が噛み合わない。(5) **表示と抽出で単位を分けた**。表示は従来どおり final ごとに即送信（遅延ゼロ）、抽出だけが発話完成を待つ。`protocol.ts` / `public/app.js` は無変更。(6) `SttAdapter.onUtteranceEnd()` は**必須メソッド**にした。`UtteranceEnd` はテキストを持たないので `TranscriptEvent` では表せず（`text: ""` は「空 transcript を送らない」不変条件と衝突する）、別経路が要る。mock は空実装。(7) **mock は 1 行を 4 語ごとの複数 final に割るようにした**。1 行 = 1 final のままだと「複数 final を1発話に統合する」という中身が mock 上で一度も発生せず、通しても素通しでテストが何も守らなかった。`mock-script.ts` は変更していない。(8) 発話**内**の連結は区切り文字なし、発話**間**は scheduler が `" "`。役割が違う。副作用として語の途中で final が割れると `AWSLambda` になりうるが、#20 と違い照合すべき元の完全文字列が存在しない（別々の Results）ため対処できない。(9) 未検証は増えた — 実機の Deepgram が日本語で `speech_final` / `UtteranceEnd` をどれだけ出すかは確かめていない。どちらも来なければ発話は 3 秒タイムアウト頼りになり、切れ目が会話の内容と無関係に決まる。

2026-08-26 update — Issue #21 のレビュー指摘に対応。核は「**セッション停止時に最後の発話が抽出に届いていなかった**」こと。(1) `session.ts` の `UtteranceBuilder` コールバックが `this.scheduler?.addUtterance()` を見ていたが、`stopSession()` はフィールドを null にしてから `builder.flush()` を呼ぶため、**flush が発行した発話が optional chaining で黙って握り潰されていた**。「builder → scheduler の順」というコメントの意図が、ローカル変数への退避とプロパティ null 化のズレで打ち消されていた。コールバックがローカルの `scheduler` を掴むよう修正。停止後の受け入れ可否は scheduler 側の `stopped` / `disabled` が判断する。(2) **`tests/session-wiring.test.ts` を新設**。このバグはテストの空白にそのまま入り込んでいた。**当初 mock の再生で書いたが検出できなかった** — mock は行の最後の final に必ず `speechFinal` を立てるので、再生を途中で止めても未確定の発話が残らない。アダプタが登録したコールバックを `mock.method` で掴んで直接叩く形に書き直し、バグを再導入すると落ちることを確認した。`ExtractionScheduler` が `setInterval` を張るので、各テストで WS の close を流して teardown させないとプロセスが終わらない点も踏んだ。(3) **`undefined` の speaker を `splitBySpeaker` と同じ規則に揃えた**。当初は `e.speaker !== this.speaker` で `undefined` を独立した話者として境界にしていたが、`split.ts` は「undefined は境界を作らず吸収」と明記しており、隣接する2層が同じ「話者不明」に逆の規則を持っていた。diarize が一時的に speaker を返さない final が1件挟まるだけで同一話者の発話が3つに割れる。(4) **transcript 空の Results に立った `speech_final` を捨てていた**のを、境界だけ `UtteranceBuilder` に伝えるようにした。捨てると確定契機が1つ黙って消える。(5) `tests/helpers/openai-env.ts` で **`STT_PROVIDER=mock` を固定**した。ローカルの `.env` に `deepgram` があると `Session` を組み立てるテストが実 Deepgram へ WS 接続しに行き、キーの有無で結果が変わる。(6) mock の `speechFinal` を「最後のチャンクの、さらに最後の1件だけ」に厳密化（`deepgram.ts` と同じ規則。`mock-script.ts` に複数話者の行を足した瞬間に静かに壊れる形だった）。`chunkMockWords` にテストを追加し、`size < 1` の無限ループもガードした。(7) 記述の訂正 — 「バッファが 120 文字に達した時点で発話の途中だろうと切る」は機構として不正確だった。`run()` はバッファ**全部**を持っていくので、切れ目は常に「最後に append したテキストの末尾」で、120 字は発火の閾値。改善の中身（末尾が final 境界 → 発話境界）は変わらない。(8) **カード表示のレイテンシ増を明記**した。発話確定の待ち（最大3秒）がスケジューラの待ち（最大10秒）に上乗せされ最悪13秒。文字起こしの表示は即時のままなので遅れるのはカードだけ。あわせて `teardown()` が発話を捨てる判断（`stopSession` と違い flush しない）、mock で `finalLines` が増える副作用、`UtteranceEnd` が対応する final より先に届きうる点も記録した。

2026-08-27 update — Issue #22「用語抽出に直前の会話コンテキストを利用する」。案A（`contextTranscript` の追加＋文字数上限のリングバッファ）で実装した。(1) `ExtractorInput` を `newTranscript`（カード化・`surfaceForms` の対象）と `contextTranscript`（語義判定だけに使う直前の会話）に分けた。user ターンは「直前の会話 → 表示済み用語リスト → 新しい文字起こし」の順で、判断対象を末尾＝直近に置く。`ROLE_PROMPT` に規則9（直前の会話にしか登場しない用語は出力しない）を足した。**system プロンプトはバイト不変のまま**なのでプレフィックスキャッシュへの影響はない。(2) `src/extract/context.ts` を**依存ゼロ**で新設（`normalize.ts` `split.ts` `utterance.ts` と同じ理由。テストが `config.ts` と `new OpenAI()` を連れ込まない）。上限は `MAX_CONTEXT_CHARS` = 1,500字で、**古いチャンクから丸ごと捨てる**（切れ目は発話の終わり＝#21 なので境界を保てる）。1チャンク単独で超えるときだけ `slice(-1500)`。**上限を「秒」ではなく文字数にしたのは、抽出側にタイムスタンプが無いから** — 時間軸を通すと `UtteranceBuilder` → `Session` → `Scheduler` の3層改修になる。(3) **`context.push(chunk)` は抽出に成功した後だけ**。一時エラーではチャンクがバッファ先頭に戻るので、先に積むと同じ発話が次回の `contextTranscript` と `newTranscript` の両方に現れる。`disableExtraction()` では `clear()` も呼ぶ。デデュープ（`shownSet`）は無変更。(4) `filterSurfaceForms()` を追加し、`surfaceForms` を `newTranscript` に実在する表記だけに絞る。規則7で指示はしていたが**サーバー側の検証が無く**、文脈を渡し始めると「直前の会話に出てきただけの表記」が混ざりうる。**置き場所は `scheduler.ts` ではなく `extractor.ts`** — 評価ハーネスは `createExtractor()` を直接呼ぶので、スケジューラに置くと評価が本番と違う挙動を測ることになる。**カード自体は落とさない**（`surfaceForms` が空でもカードは残す。ハイライトが効かないだけ）。純関数として export したので LLM 抜きで固定できる。`includes("")` が常に true になる穴も明示的に塞いだ。(5) 評価基盤（#18）に `TermCase.context` と `EvalConfig.useContext` を追加。`EVAL_NO_CONTEXT=1` で文脈を空にして流し、**同じ fixture で文脈あり/なしを比較**できる。`0`/`1` 以外は例外にする（`EVAL_NO_CONTEXT=true` を黙って無視すると「文脈なしで測ったつもり」のレポートができる）。fixture に合成2ケース（`context-disambiguation` / `context-no-recard`）を追加。(6) テスト3本を新設（`context-window` / `surface-forms` / `scheduler-context`）。本題は**一時エラー時に文脈へ積まないことの回帰テスト**で、private の `extract` フィールドを差し替えて LLM を呼ばずに回す（`enrichCard` も潰さないと `void` の投げっぱなしで実 API に飛ぶ）。(7) `scheduler.ts` のクラス docstring が発火条件を「20秒経過」と書いていたのを実際の `MAX_WAIT_MS` = 10秒に訂正した（Wiki は訂正済みでコードだけ残っていた）。(8) 未検証 — 文脈に引きずられた誤補正が増えないか、入力トークン増（1,500字 ≒ 1,100トークン）でカード表示のレイテンシが #21 の実測（最悪13秒）から悪化しないかは、実 API での評価と実機確認が要る。

2026-08-27 update — Issue #22 のレビュー指摘に対応。核は「**単体では正しい部品が、配線だけ抜けても誰も気づかない**」こと。(1) `extract()` の中で user ターンを組んでいたため、`contextTranscript` を捨てても `filterSurfaceForms()` の戻り値を捨てても**新規テスト19本が全部通る**状態だった（レビュー側が変異テストで確認）。`buildUserTurn()` を純関数として切り出して並び順と `(なし)` フォールバックを固定し、さらに `client` を export して `chat.completions.parse` だけ差し替える配線テストを1本足した。実 API は叩かない。3種類の変異（フィルタ未適用・文脈を渡さない・区切りを予算に数えない）がすべて落ちることを確認済み。(2) `ContextWindow` の予算計算が `" "` 区切りを数えることも無テストだった（`return n;` に変えても全通過）。上限ちょうどで残す／区切り1文字ぶんで超えて捨てる、の差が出るケースを追加。(3) `push()` のガードを `text === ""` から `text.trim() === ""` に変え、`buildUserTurn()` 側も `context.trim().length > 0` に揃えた。空白だけのチャンクが「(なし)」判定をすり抜けて空白としてプロンプトに入る経路があった。(4) `flagEnv` の trim を他の env ヘルパに揃え（`EVAL_NO_CONTEXT=" 1"` だけ例外になっていた）、`useContext` の二重否定を解消。(5) [[termlens-open-issues]] に「**AC『過去文脈の用語を再カード化しない』は CI では検出できない**」を明記。`surfaceForms` の出所は決定的に担保できたが、こちらはプロンプト規則9と `shownTerms` だけが支えで、検出手段は課金を伴う `context-no-recard` ケースしかない。機械的に落とす案は正当な補正カード（「クバネテス」→ `Kubernetes`）を巻き添えにするので採らない。

2026-08-27 update — Issue #23「誤認識補正を候補生成・検証の2段階にする」。案B（Stage 2 を新設せず既存の清書に相乗り）で実装した。(1) **最初に確かめたのは「`responses.create` で `web_search` と構造化出力を併用できるか」**で、設計時点では未検証だった（駄目なら `CHOSEN:` 行のパースにフォールバックする想定）。実 API で確認したところ**併用できる** — `web_search_call` が実行されたうえで `output_text` に JSON が返り、`url_citation` の annotations も従来どおり付くのでリンク収集は無変更で済んだ。ただし **`description` には指示に反して引用記法が混ざる**ので `stripInlineCitations()` / `clampDescription()` は構造化出力でも通している。(2) `TermCardSchema.candidates`（`{term, reading, rationale}` 最大3件）を追加し、不変条件を **`candidates[0].term === term`** に固定した。整形は `normalizeCandidates()`（純関数）で、空 term を落とし正規化キーで畳み、`term` を先頭へ移し（表記は `term` 側で上書き）、**先頭を差し込んだ後に**切り詰める。**上限をサーバー側でも切るのは体裁の問題ではない** — 超過ぶんが `max_completion_tokens`(3000) を圧迫すると JSON が途中で切れて `parsed` が null になり、そのチャンクのカードが全部消える。(3) `candidates` は **`protocol.ts` に載せない**。`scheduler.ts` の送信が `{ ...c, links: [], willEnrich }` のスプレッドなので、分割代入で明示的に落としている（放っておくと漏れる）。(4) `enrichTerm()` を `verifyAndEnrich()` に置き換え（呼び出し元は1箇所なので互換用は残していない）。パースは `parseVerifyOutput()` に切り出し、**`chosen` は候補の中からしか採らない** — 検証段で新しい用語を作れると独立した検証者を立てた意味が無くなる。(5) 検証対象は `selectVerifyTargets()` で「**補正あり or confidence low**」∪「レア度上位の約半数」の和集合。評価ハーネスも**同じ関数を呼ぶ**（コピーすると本番の条件を変えたときに評価だけ古い条件のまま緑になる）。(6) 棄却は **既定 (c)「表示を変えない」**。`console.warn` に `term` と `reason` だけを出し、**文字起こし本文は出さない**。**候補#2 が選ばれた場合も表示を変えない** — `card_update` は term でカードを突き合わせるので改名の経路が無い（判定は `isVerified()` に集約。設計コメントは「chosen あり → 差し替え」としていたが、protocol の制約から裏付け無し扱いに倒した）。(7) `EVAL_WITH_VERIFY=1`（既定 false、`flagEnv()` の作法）で Stage 2 まで通す2モードにした。プロンプト変更と検証は別々に悪化しうるので、まとめて測ると原因が分からない。**裏付けの取れなかったカードは指標から落とす**（測りたいのは検証の判断そのもの）。**誤補正率だけ見ると過剰棄却を見逃す**ので `expectCorrection` とセットで読むこと。(8) テスト3本を新設（`candidates` / `verify-parse` / `scheduler-verify`）。実 API は叩かず、`enrich.ts` の `client` を export して `responses.create` を差し替える形にした（**`enrichCard` は `void` の投げっぱなしなので潰さないと実 API に飛ぶ** — #22 で踏んだ）。`candidates` の送信漏れは変異テストで落ちることを確認済み。(9) fixture に合成2ケース（`lookalike-ansible` / `lookalike-confluence`）を追加。狙いは「実在しない語を弾く」ではなく「**実在するが文脈に合わない語**を弾く」ほうで、Stage 2 に web 検索という独立した情報源を持たせた理由そのもの。(10) 範囲外 — word confidence の利用（4層改修になる）、unresolved 状態の表示。(11) 未検証 — 誤補正率と正しい補正率が実 API でどう動くかは `npm run eval:llm` を実装前・実装後既定・`EVAL_WITH_VERIFY=1` の3回回して比較する必要がある。過剰棄却がリスクとしては最大。(12) 昇格候補 — 「OpenAI Responses API では web 検索ツールと構造化出力を併用でき、annotations も残る」は他プロジェクトでも効く知見なので `~/wiki/knowledge/` 側の更新が要る（現行ページは Anthropic 時代の記述のまま）。

2026-08-27 update — Issue #23 のレビュー指摘に対応。核は「**直前のエントリに書いた `max_completion_tokens` 超過時の挙動が事実と違い、その誤解のまま無限リトライを作っていた**」こと。(1) 上のエントリの「JSON が途中で切れて `parsed` が null になり、そのチャンクのカードが全部消える」は**誤り**。`node_modules/openai/lib/parser.js` は `finish_reason === "length"` のとき `LengthFinishReasonError` を**投げる**。しかもこれは `APIError` ではなく `OpenAIError` の直下なので `isPermanent()` は false を返し、素通しにすると**同じ長さのチャンクをバッファ先頭に戻して再送し続ける**（確実に同じ所で切れるので永久に抜けない）。`isUnretryableChunk()` を足してそのチャンクだけ捨て、**連続失敗にも数えない**（次のチャンクは通りうるので抽出を止める理由にならない）。`LengthFinishReasonError` はパッケージのルートから再エクスポートされていないので `openai/error` から import する。`normalizeCandidates()` は**パース成功後**に走るため、そもそも超過を防げないことも4箇所のコメントで訂正した。(2) **棄却時に永久スピナーが残っていた**。速報を `willEnrich: true` で送っている以上、クライアント（`public/app.js`）は `card_update` が来るまで「確認中」の表示を畳まない。棄却で `return` すると**誤補正が最も疑わしいカードにだけ**回り続ける。解説はそのまま・リンクは空で `onCardUpdate` を送るようにした（表示テキストは動かない）。これを `h.updates` が `[]` だと主張していたテスト2本は、古い（壊れた）意図をそのまま固定していたので書き直した。(3) **評価ハーネスだけ候補#2 を採用して差し替える**ようにした。本番は `card_update` の term 突き合わせで改名できないが、評価では棄却として落とすと「正しく選び直せた」ケースが Precision の分母から消え、**`EVAL_WITH_VERIFY=1` が構造的に検証の効果を示せない**。あわせて1カードの検証失敗で試行ごと捨てないよう例外を包み、棄却・差し替え・失敗の件数を `VerifyTally` として `EvalReport` と `formatTable` に出した（Precision は分母が用語数なので、カードを落とすと数字だけ良く見える）。(4) テストの検出力を変異で確かめて埋めた — `selectVerifyTargets` のテストは**レア度上位の枠が空いていた**ため、追加した「補正あり or low」の条件を消しても落ちなかった（rare を3枚並べて枠を埋めた）。検証に回るカード数と `willEnrich` のアサーションも追加。「選定条件2つを個別に消す」「全カードを検証に投げる」「`willEnrich` を常に false」「棄却時に `card_update` を送らない」の4系統がすべて落ちることを確認済み。(5) `parseVerifyOutput()` の JSON 切り出しを `lastIndexOf("}")` から**括弧対応のスキャン**に変えた（文字列リテラル内の括弧は数えない）。後ろに別のオブジェクトや `}` を含む後書きが続くと巻き込んで丸ごとパースに失敗する。(6) クライアント向けカードの生成をスプレッドから**フィールドを1つずつ書き写す `toClientCard()`** に変えた。`{ ...card }` では `candidates` の除外がコンパイラに守られず、`ExtractedCard` に内部用フィールドが増えるたび黙って漏れる。(7) 評価の env フラグ解決を**片方向**に直した（`EVAL_DEFAULTS.x && !flag` のままだと既定を反転したときにフラグが効かなくなる）。(8) Wiki の訂正 — [[termlens-term-extraction]] の「1用語あたりの検索は最大1回（`max_uses: 1`）」は `enrich.ts` に該当する指定が無く事実でない。検索回数を縛っているのは対象カードの絞り込みだけ。

2026-08-27 update — Issue #23 の `/simplify`（再利用・単純化・効率・実装の深さの4観点）に対応。(1) **評価の合否ゲートが本番より甘い側に構造的にずれていた。** 「候補#2 を採用して差し替える」を評価だけに入れた結果、本番なら `miscorrections` に数えられるカードが `correctionHit` に化け、`maxMiscorrection` の判定が本番と別のシステムの数値で決まっていた。**そもそも現行の protocol では Stage 2 は term ベースの指標を一切動かさない**（改名できず、棄却しても速報カードは残る）ので、評価の既定も本番と同じくカード集合を変えないようにし、検証が何をしたかは `VerifyTally` だけが伝える形にした。差し替え・棄却の適用は `EVAL_ALLOW_RENAME=1` の探索モードへ分離し、`formatTable` にどちらで回したかを出す。(2) **`useContext` のフラグ書き換えは取り消した。** 前回「片方向に直した」と書いたが、旧 `EVAL_DEFAULTS.useContext && !flagEnv(...)` と新形は真理値表が4通りとも一致し、**存在しない差をコメントで主張していた**。#23 と無関係な行でもある。`withVerify` 側の死んだ `||` だけ残す形に戻した。(3) テストのファクトリを `tests/helpers/cards.ts` に集約（`card` / `candidate` / `utterance` / `settle`）。4ファイルに散っていて、#23 で `candidates` を1つ足すのに2ファイルを機械的に直していた。`utterance()` は `MIN_CHARS`(120) を `"あ".repeat(130)` の形で暗黙に持つので、コピーが残ると**閾値変更で片方だけが発火しなくなり緑のまま**になる。集約時にカードと候補の `reading` を同じ値に潰したら「補った候補の読みをカードから採る」テストの検出力が消えたので、ヘルパ側で別の値に分けた（潰した瞬間にテストが落ちたので気づけた）。(4) 清書側でも恒久エラーを見るようにした。抽出は `disableExtraction()` で止まるのに `enrichCard` の catch はログだけで、残高切れでも選ばれたカードごとに web 検索つき呼び出しを投げ続けていた。#23 で対象集合を広げたぶん影響が大きい。(5) `verifyAndEnrich` に `status: "incomplete"` の判定を追加。Responses API は出力長超過を例外ではなくこの形で返すので、見ないと**抽出段では一級の失敗モードとして扱っている出力長超過が、検証段では「JSON として返らなかった」という汎用エラーに化けていた**。あわせて棄却時は早期リターンし、使われないリンク収集30行を走らせないようにした。(6) `selectVerifyTargets` の並べ替えの第2キーは、和集合を入れた結果**コメントの主張と実効果が逆**になっていた（low はどのみち全部入るので、タイブレークの唯一の効果は「low に上位半数の枠を食わせて対象を狭く保つ」こと）。挙動は有用なので残し、コメントを実態に合わせた。(7) `normalizeCandidates` を Map 一本に単純化（`findIndex` の二重 `normalizeTerm`、`slice` 結合、2つの三項が消えた）。リンクの並べ替え順位を候補生成時に1度だけ計算するようにした（比較関数の中だと `new URL()` と正規表現が O(n log n) 回走る。`include` を付けたので候補は引用済みの数件ではなく検索結果の全件）。`zodTextFormat` の変換もモジュール定数へ巻き上げ。(8) `parseVerifyOutput` の JSDoc が `extractJsonObject` の挿入で迷子になっていたのを戻し、`String(outputText ?? "").trim()` の三重の無駄を削り、`enrich.ts` の「新しい LLM 呼び出しは増えない」を「1カードあたりは増えないがチャンクあたりでは増える」に訂正した。(9) `isUnretryableChunk` にテストを追加（`isPermanent` が false を返すことも固定した。ここが崩れると無限リトライが戻る）。(10) **見送った指摘** — `client.responses.parse()` への移行（`output_parsed` は `status` が `completed` 以外だと**例外ではなく null** になるので失敗の見え方が変わる。実 API で検証済みなのは `create` 経路であり、その検証をやり直さずに差し替えない）、エラー分類を `classifyExtractionFailure()` に一般化する案（`isPermanent` / `isQuotaExhausted` は #23 以前からの仕組みで、400 をチャンク固有に振り直すのは別 Issue の範囲）、棄却が見込まれるカードだけ検索結果の同梱を外す案（コスト設計の変更）、`selectVerifyTargets` の上限キャップ（挙動変更）。(11) 変異テストで確認 — 重複畳み込みの正規化を外す/切り詰めをやめる/先頭の表記を上書きしない/棄却時の早期リターンを外す/棄却でも検証側の解説を送る、の5系統がすべて落ちる。

2026-08-27 update — Issue #24「unresolved 状態を導入して不確実な用語を断定しない」。案B（`confidence` を `status` へ**置き換え**、改名はしない）で実装した。(1) `TermCard.confidence: "high" | "low"` を **`status: "confirmed" | "probable" | "unresolved"`** に置き換え（`low` → `probable` が 1:1）。**追加ではなく置き換え**なのは、併存させると同じ事実を2フィールドで持ち、`high` かつ `unresolved` のような矛盾した組の正解が決まらないため。`card_update` にも `status` を載せた（optional にしない — 送り側では必ず決まるので、省略可にすると受け側が「変化なし」と「未指定」を区別できない）。(2) **`src/stt/` の `confidence` は word confidence で別物なので触っていない**（Deepgram の語単位スコア）。一括置換は禁止で、`src/stt/` と `tests/{split-by-speaker,transcript-events,transcript-words,mock-words}.test.ts` は対象外。(3) **改名はしない。降ろしたのは表示の主役だけ。** `term` は `dataset.term`・`cardData` のキー・デデュープのキーとして残し、`unresolved` のときだけ見出しを `surfaceForms[0] ?? correctedFrom ?? term` にする。`card_update` が term で突き合わせる以上、改名の経路が無いという #23 の制約は変えていない（再キーは別 Issue）。(4) **`unresolved` から上へは戻さない。** Stage 2 が候補#2 を選んでも `unresolved` に倒す。再接続でカードが再送されたときも `status` は上書きしない（速報の判断で格上げしてしまうため）。(5) サーバー側の担保として `normalizeStatus()` を追加し、`unresolved` の `description` を `UNRESOLVED_DESCRIPTION` に差し替える。`extract()` は `filterSurfaceForms` → `normalizeCandidates` → `normalizeStatus` の順。**LLM の従順さに依存しない**という `filterSurfaceForms()` 以来の方針で、ここが素通しだと「特定できていないのに別用語の説明を断定的に読ませる」害がそのまま出る。(6) `selectVerifyTargets()` の条件を **`status !== "confirmed"`** に。値を列挙（`=== "probable"`）すると `unresolved` が黙って漏れ、特定できないと言ったカードが一度も独立した情報源で確かめられなくなる。(7) クライアントの導出は **`public/card-status.js` の `cardStatus()` / `cardHeading()` に閉じた**。描画も Markdown もここだけを見る（`card.status` を直接読む箇所を作らない）。`status` が無い旧カードは `confidence` から導出する（localStorage の後方互換）。`app.js` から切り出したのは Node のテストから読むためで、`public/lowpass.js` と同じ理由。(8) `metrics.ts` の名前衝突を解消 — 旧 `unresolved`（＝カードは出たが `confidence: low`）は **名前ごと `probable` へ移し**、`unresolved` を新設して `formatTable` も2列にした。同名で意味を変えると実装前後のレポート比較が静かに壊れる。評価ハーネスの既定モードでも **`status` だけは本番と同じに動かす**（term は変えないので合否ゲートには影響しない）。(9) テスト2本を新設（`normalize-status` / `card-status`）。fixture に「特定できないのが正解」の合成2ケース（`unresolved-garbled-product` / `unresolved-lookalike-noise`）を追加し、音韻が近いだけの実在用語を `forbidTerms` に置いた。(10) **変異テストで6系統の検出を確認** — `normalizeStatus` の素通し / 棄却時の status を confirmed に / `cardStatus` の後方互換の導出を落とす / `selectVerifyTargets` を probable だけに / `cardHeading` を term のままに / metrics で unresolved を probable に混ぜる、がすべて落ちる。(11) 判断が残っている点 — **棄却（Stage 2 の降格）では `description` を速報のままにしてある**。見出しとリンクが同時に変わるうえ本文まで差し替えると表示が丸ごと別物に見えるためで、文言の最終形は実機確認（#24 の人タスク）で決める。`allowLowConfidence`（fixture のキー名）は `confidence` 時代の名前のままにした — 状態ではなく用語の集合を指すフィールドで、改名すると過去レポートとの比較がしづらくなるため。(12) 未検証 — 過剰 unresolved になっていないかは `npm run eval:llm` を実装前後で回し、**unresolved 率・probable 率・Recall・正しい補正率をセットで**読む必要がある。unresolved カードの文言と見た目、`probable → unresolved` の降格が視覚的に唐突でないかは実機確認が要る。

2026-08-27 update — Issue #24 のレビュー指摘に対応。核は「**AC を担保する仕組みが Stage 1 にしかなく、Stage 2 の降格経路が素通しだった**」こと。(1) [高] **降格したカードが「特定できませんでした」と言いながら別用語の断定的な解説を出していた。** 速報の解説は誤補正した用語の説明なので、見出しが surface form に替わったぶん「何の説明なのか分からない断定」だけが残る。抽出段の `normalizeStatus()` が同じことをサーバー側で担保している以上、降格経路だけ素通しにするのは非対称。`UNRESOLVED_DESCRIPTION` に差し替えるようにした。(2) [高] **見出しに出せる材料が無い unresolved を落とすようにした。** 見出しは `surfaceForms[0] ?? correctedFrom ?? term` なので、どちらも無いと**推定した term がバッジ付きで見出しに出る** — この Issue が最も避けたい「誤った実在用語への断定」がそのまま起きる。`filterSurfaceForms()` は文字起こしに実在する表記しか残さないため、表記が僅かにズレただけで surfaceForms は空になりうる（`surface-forms.test.ts` の「空になってもカードは残す」経路）。(3) [中] **検証が例外で落ちると「確認中」スピナーが永久に回っていた。** #23 で棄却時に塞いだのと同じ穴が例外パスに残っており、**#24 で `unresolved` を検証対象に足したぶん被弾面積が広がっていた**。catch でも速報の status と解説のまま `card_update` を送る。恒久エラーで打ち切った後は `willEnrich: false` で送る（誰も更新を送らないため）。(4) [中] **`updateCard` が unresolved を昇格させていた。** `addCard` の再送経路にはガードがあり wiki にも「上書きしない」と書いてあったが、再抽出されたカードは再び Stage 2 に回るので `card_update` 経由で昇格していた（LLM + web 検索なので非決定的）。(5) [中] **unresolved の推定 term が `shownTerms` を汚染していた。** プロンプト規則2「表示済みの用語は出力しない」が効き、後で誰かが同じ用語を明瞭に発話しても正しいカードが出なくなる。`shownSet` には積むが `shownTerms` には載せない形にした。(6) [中] **評価が #24 の効果を原理的に測れない形だった。** `forbidTerms` / `expectCorrection` は `card.term` だけを見るので、Stage 2 が正しく棄却しても誤補正として数え続ける → `status !== "unresolved"` のカードに限定。さらに新規 fixture は `expectTerms: []` なので `unresolved` 率の分子にも分母にも入らず、**追加したケースが指標を1ミリも動かせなかった** → `expectUnresolved`（聞き取られた表記の集合）と `unresolvedRecall` を新設。(7) [中] **`app.js` の DOM ロジックが1行もテストされておらず、AC「Markdown エクスポートにも状態が残る」を検証するテストが存在しなかった**（状態注記を空文字にしても全テストが通る状態）。`buildTermsMarkdown()` を `public/terms-markdown.js` に純関数として切り出し、テスト8本を追加。(8) [低] `cardStatus()` を3値に丸めるようにした（戻り値がそのまま `classList.add()` に渡るので、壊れた localStorage で DOMException → 復元の catch がセッションを丸ごと消す）。`updateCard` の合成フォールバックを削除（`surfaceForms` が空になり unresolved の見出しが推定 term に落ちる枝だった）。`.card.unresolved.flash` を追加（文字起こしの語をタップして飛んだ直後に下地が通常カードと同じになっていた）。**`status` をスキーマで `term` より前に移した** — 構造化出力はスキーマ順に生成されるので、term を書き終えてから status を決めさせると規則1の「無理に確定しない」が原理的に守りにくい。(9) 変異テスト10系統すべて検出を確認 — 降格時に定型文へ差し替えない/材料の無い unresolved を落とさない/例外時に card_update を送らない/打ち切り後も willEnrich: true/unresolved を誤補正に数える/断定しても当たりにする/Markdown の状態注記を落とす/Markdown の見出しを term のままに/status を3値に丸めない/unresolved も shownTerms に載せる。

2026-08-27 update — PR #33（Issue #24）の自動レビュー指摘に対応。核は「**`unresolved` を検証に回すこと自体に意味が無かった**」こと。(1) [高] `updateCard()` が `status` の昇格だけ止めて `description` / `links` は無条件に上書きしていた。Stage 1 が `unresolved` としたカードも検証対象に入っていたので、Stage 2 が裏付けを取ると「特定できませんでした」の見出しの下に**確定した別用語の断定的な解説**が出る — 降格側で定型文への差し替えを徹底したのに、昇格を止める側が非対称だった。リンクは画面には出ないが `buildTermsMarkdown()` は status を見ずに出すので、Markdown には「特定できませんでした」＋確定リンクが並ぶ。(2) 表面的には「据え置くなら解説も据え置く」で済むが、**より深い原因は `unresolved` を Stage 2 に回していたこと**。昇格の経路が無く（`card_update` は term で突き合わせる）解説も定型文で固定される以上、検証結果を使える余地が1つも無い。`selectVerifyTargets()` が最初に `unresolved` を除くようにした（レア度ランキング経由でも入らない。ただし枠の分母は元のカード数のままにして、unresolved が多い回に probable が漏れないようにする）。web 検索の呼び出しも減る。(3) 表示側は多層防御として残し、畳み込みの判断を `mergeCardUpdate()`（`card-status.js` の純関数）に切り出した。**この不変条件は DOM を触らずに固定できる**ので、テスト5本で押さえた。(4) 変異テスト3系統で検出を確認 — unresolved も検証対象に入れる/unresolved でも解説を上書きする/**status だけ据え置き解説は上書き（指摘そのものの形）**。

2026-08-27 update — PR #33 の再レビュー指摘に対応。**同じ穴が `addCard` の再送経路に残っていた。** `card_update` 側は `mergeCardUpdate()` で塞いだが、再接続で Stage 1 が再抽出したカードが `cards` として届く経路では `status` だけ据え置いて `description` を通していた（`existing.links.length === 0` の分岐）。unresolved カードは常に `links` が空なので**この分岐に必ず入る**。結果は指摘1件目と同じ — 「特定できませんでした」の見出しの下に断定的な解説が出る。教訓は「**降格したカードへ更新が届く経路が2つある**」ことで、片方だけ塞ぐともう片方から同じ形で漏れる。判断を `shouldApplyResend()` として `card-status.js` に切り出し（`mergeCardUpdate()` と対称）、テスト5本で固定した。変異2系統で検出を確認 — unresolved のガードを外す（指摘そのもの）/清書済みのガードを外す。

2026-08-27 update — PR #33 のローカルレビュー（CI のレビューが `error_max_turns` で落ちて判定が得られなかったための代替）に対応。核は「**2度指摘された不具合そのものを `app.js` に書き戻しても全テストが緑のまま**」だったこと。(1) [中] `mergeCardUpdate()` / `shouldApplyResend()` の**中身**はテストで押さえてあったが、「`app.js` がそれを呼んでいる」ことを見ているテストが1本も無かった。変異3系統（素の `Object.assign` に戻す/再送の旧条件に戻す/引数の `description` を直接描く）がいずれも無検出。#22 で踏んだ「単体では正しい部品が、配線だけ抜けても誰も気づかない」の再来。`app.js` をソース文字列として読む `tests/app-wiring.test.ts` を新設した。壊れやすいテストだが**壊れたときに直すべきなのは呼び出し側**なので誤検知にはならない。`document` を触る `app.js` は Node から import できず、jsdom はビルドレスの方針に対して重い。なお「本文を畳み込み後のカードから描く」は該当行が `addCard` / `updateCard` の2箇所にあるため、良い形の存在ではなく**悪い形の不在**で判定する（存在だけ見ると片方が残っていて通る）。(2) [低] `selectVerifyTargets()` の枠の分母について、直前のコミットで書いたコメント「unresolved が多い回に枠まで縮むと probable が漏れる」は**誤り**だった。直下のループが `probable` を無条件に足すので漏れない。実際の効果は逆で、元のカード数を分母にすると `unresolved` が多い回ほど枠が余り、**検証する意味のない confirmed まで web 検索に回る**（「並べ替えは対象を狭く保つためにある」という方針と逆向き）。分母を `targetable.length` に直した。(3) [低] ループ条件を `status === "probable"` から `status !== "confirmed"` に戻した（`targetable` の時点で unresolved は除いてあるので意味は同じだが、将来 status が増えたときに黙って漏れない）。(4) [低] 陳腐化したコメント2箇所を訂正 — `app.js` の「検証には回るが昇格の経路が無い」、`card-status.js` の「この経路は本来来ない」。後者は**現行サーバーでも来る**（再接続で `shownTerms` が 50 件に切り詰められ再抽出されると、サーバーから見たその用語は `probable` なので Stage 2 に回る）。多層防御ではなく本線のガードだった。

2026-08-27 update — Issue #25(案C)を実装。**AC 6件のうち4件は #23・#24 で実装済み**だったので、実体は既存の `verifyAndEnrich()` に「判断の内訳」と「回数の上限」を足すこと。4段階で入れた。(1) **計測** — `verifyAndEnrich()` の戻りに `searches`(`response.output` の `web_search_call` 件数)を追加し、`VerifyTally` と `formatTable` に出した。件数の述語 `isWebSearchItem()` はリンク収集と**共有**する(別々に書くと、リンクは集まっているのに検索回数だけ 0 と報告される一番たちの悪いズレ方をしうる)。(2) **検証と解説の分離(AC4)** — `VerifyResultSchema` に `verification { exists, fitsContext, evidence }` を足し、`SYSTEM` を「## 候補の検証」「## 解説の作成」の2節に書き分けた。**`exists` と `fitsContext` を分けるのが核心** — `chosen: null` の一本槍では「実在しなかった」のか「実在するがこの会議の話ではなかった」のかが区別できず、#24 で最大リスクとした過剰 unresolved の原因分析ができなかった。`normalizeVerification()`(純関数)が **exists か fitsContext が false なら `chosen` を null に倒す**(`normalizeStatus()` と同じ「LLM の従順さに依存しない」方針)。**`chosen` の決定ロジックそのものは変えていない** — 候補の中からしか採らないという #23 の不変条件はそのまま。`VerifyTally.rejected` は `rejectedNotExist` / `rejectedOffContext` に割った。(3) **用語集の関連語(AC2)** — `relatedGlossary()`(純関数)が候補と表記の近い語だけを最大5件返す。**用語集は参加者名・社名を含み(`ROLE_PROMPT` 規則4)、web 検索は外部サービスへの送信**なので全件は渡さない。実装中に**候補の `reading` で突き合わせると「読み: テスト」が「株式会社テスト工業」を引き寄せる**のをテストで観測し、reading は見ない方針に倒した(関連語を取りこぼしても判断材料が1つ減るだけだが、社名が外部検索へ乗るのは取り返しがつかない)。配線は `scheduler.ts` と評価ハーネスの2箇所(評価に通さないと `relatedGlossary()` が常に空になり、評価だけ本番と違う入力を測る)。(4) **検索回数の上限(AC5)** — `max_tool_calls`。**値は暫定で、人が計測結果を見て決める**。暫定値をわざと緩め(5)に取ってあるのは、上限そのものが検索を打ち切ると**上限値を決めるための分布を上限が壊す**ため。`max_tool_calls` は SDK 7.4.0 の stable な型に無いので、`SEARCH_LIMIT` としてこのキー1つだけ型を広げて渡している(`as any` にはしない)。変異7系統で検出を確認 — `normalizeVerification` を素通し(2本落ちる)/棄却の内訳を合算(1本)/`relatedGlossary` の絞り込みを外す(10本)/`max_tool_calls` を落とす(1本)/scheduler の glossary 配線を落とす(1本)/`searches` の計測を落とす(3本)/評価の `searches` 集計を落とす(1本)。**人に残した判断**: `MAX_WEB_SEARCHES` の値決め、`unresolved` 率・`unresolvedRecall` の実測、`max_tool_calls` を実 API が受け付けるかの確認(400 が返ると `isPermanent()` でそのセッションの検証が丸ごと止まる)。

2026-08-27 update — Issue #25 のレビュー指摘に対応。**同じ穴が2箇所で見つかった**。(1) **`relatedGlossary()` の絞り込みがほぼ効いていなかった** — `normalizeTerm()` が空白を落とすので用語集エントリが1本の文字列に潰れ、**語の境界が消える**。候補 `Go` が `Kengo Yamada` に、`AI` が `Aiko Tanaka` に、`SAS` が `Ken Sasaki` に当たり、**ローマ字の氏名が丸ごと外部検索へ渡っていた**。実装中に「読み: テスト」→「株式会社テスト工業」を観測して `reading` を突き合わせから外していたが、**原因は読みではなく境界の無い部分一致**だったので `term` 側で同じことが起きていた（**入力を1つ減らしても機構は残る**）。突き合わせを語単位（区切り記号で分割 → 完全一致 or 4文字以上の語頭一致）に変え、語数の多いエントリは当たった語だけに切り詰めるようにした。教訓: **絞り込みを入れたら、絞られる側の実データ形を実際に通して確かめる** — 「無関係な語が渡らない」テストは合成データが日本語の氏名だけだったので、10本あっても1本も落ちなかった。あわせて Wiki の「氏名が外部へ出ない」という断定を緩めた（`context`(文字起こし抜粋)経由で同じリクエストに載るので、これは**1経路の多層防御**であって保証ではない）。(2) **soft cap と hard cap を同じ数にしていた** — `max_tool_calls` に達すると API はその場で応答を打ち切るので、SYSTEM の「N回で取れなければ取れなかったものとして扱う」が発火する前に JSON が途切れ、`enrichCard` の catch へ落ちて**速報の断定的な解説がそのまま残る**（上限を入れたことが、上限とは無関係な失敗モードを作っていた）。hard cap = soft cap + 2 に分離。(3) **`MAX_WEB_SEARCHES` を env ノブに** — 緩めの暫定値を入れたまま測っても8回撃ちたかったケースは観測できない（**上限値を決めるための分布を上限が壊す**）。`MAX_WEB_SEARCHES=0` で soft/hard 両方が外れる無検閲ベースラインを取れるようにした。(4) **棄却の内訳が候補外の棄却を誤分類していた** — 棄却の理由は3つあり、`out-of-candidates` は `parseVerifyOutput()` 側の判断なのでモデルは `exists: true, fitsContext: true` と言ったまま。`verification` から内訳を導くと「文脈に合わなかった」に化け、ログにも自己矛盾した行が出ていた。`RejectionKind` として明示的に返す形に変え、見出しは依存ゼロの `src/extract/rejection.ts` に置いて本番ログと評価レポートで共有。(5) **`verifyDisabled` が無言だった** — 抽出は `disableExtraction()` が通知するのに検証だけ黙って止まり、以降ずっと未検証のカードが出続けることを誰も知れなかった（`willEnrich` が false になるので確認中の表示すら出ない）。`onError` で通知（`permanent` は立てない — 抽出は生きている）。変異13系統で検出を確認。**`max_tool_calls` の 400 リスクにコードのフォールバックは足していない** — パラメータ自体は SDK の beta 側の型に実在するので確率は低く、確実な対処は**確認をマージ前の実 API 1回に前倒しすること**（人タスクの計測ランがそのまま兼ねる）。

2026-08-27 update — Issue #25 の simplify レビュー(4観点)に対応。**閾値のスカラーが増えたら機構が足りていない印**、という形の指摘が中心だった。(1) **`relatedGlossary()` の絞り込みを「語として完全一致」1本に畳んだ** — 1回目の修正で足した `MIN_PREFIX_MATCH`(4文字以上なら語頭一致)と `MAX_ENTRY_WORDS`(2語以下はエントリ全体を返す)は、どちらも**観測された事例が落ちるように選んだ数**でしかなく、前者は4文字以上のローマ字氏名(多数派)に効かず、後者は `Qdrant 山田` を丸ごと通した。区切りの無い複合語は `splitWords()` が**スクリプトの変わり目**で割るようにし、**返すのは当たった語だけ**にしたら、スカラーが2つとも消えて規則が1本になった。(2) **`verifyAndEnrich()` の口を `glossaryHints`(絞り込み済み)に変えた** — `glossary: string[]`(全件)のままだと、氏名を外へ出さない保証が「呼び出し側が絞り込みを通す」という**規律だけ**になる。型で口を絞れば素通しは書けない。あわせて用語集を語に割った索引を `ExtractionScheduler` のフィールドとして**セッションに1度だけ**作るようにした(カードごとに割り直すと100語で `normalize()` が毎回400回超。`VERIFY_FORMAT` と同じ扱いに揃えた。モジュールレベルのキャッシュにすると会議が終わっても氏名を掴んだままになるので置き場所が重要)。(3) **差し替えを「理由なしの棄却」としてログしていた**(レビューが見つけた実バグ) — ログの分岐は `!isVerified()` なので候補#2 の差し替えでも通り、そのとき `rejection` は null なので `(理由なし)` と出ていた。`rejection.ts` の doc が「自己矛盾した行が出る」として塞いだのとまったく同じ形を、別の場所に作っていた。(4) **`enrich.ts` の re-export を削除** — `rejection.ts` は「`new OpenAI()` を引き込まずに読める」ために切り出したのに、re-export が**保証の逆な第2の輸入経路**を作っていた(`normalize.ts` は re-export していない)。(5) **同じ説明の書き写しが既に drift していた** — `VerifyTally` を2フィールドから `Record` に変えたとき、説明を写した先を4箇所直し漏れていた。皮肉なことに「値の決め方をこのコメント以外に書き写さないこと」と書いた当のコメントを `config.ts` に写していた。コメント量も追加行の62%(リポジトリの通常は30〜39%)だったので、手順は Wiki の1箇所に集約して削った。(6) テストヘルパーを `tests/helpers/verify.ts` に集約(応答モックは `JSON.stringify` を通るので**型検査が効かず**、直し漏れると緑のまま実装とズレる)、`EvalReport` の手組みを実物に、`.env.example`/README に `MAX_WEB_SEARCHES` を追記、env の検証を `intEnv()` に一本化(小数を通すと `max_tool_calls: 4.5` で API 400)、評価レポートに**実効の上限値**を記録(`model` を実効値で残しているのと同じ理由)。変異は新規10系統を含め全て検出を確認。**見送った3件**(用語集ポリシーのセッション境界への引き上げ、検証結果の判別共用体化、検証未完了時の降格経路)は [[termlens-open-issues]] に記録。


2026-08-27 update — Issue #26「実機向け収音モードと音声診断情報を追加する」。案B（モード選択＋`getSettings()` 表示＋音声統計＋エクスポート）で実装した。(1) **既定モードの constraints を1バイトも動かさないことを最初に固定した**（AC6）。`public/capture-mode.js` を依存ゼロで新設し、`app.js` からは constraints の値を消して `audioConstraints(mode)` の呼び出しだけにした。`tests/capture-mode.test.ts` の期待値は**定義元から import せずリテラルで書く** — import すると定義を変えたときに一緒に動いて何も守らない。あわせて `app.js` に `echoCancellation:` 等が**書き戻されていないこと**を `app-wiring.test.ts` で見る（ベタ書きが復活すると、テストが見ていない値で実際のマイクが開く）。(2) **`getSettings()` は除外ではなく採用リストで拾う**。`deviceId` / `groupId` は端末ごとに安定した識別子で、会話本文でも音声でもないがエクスポートすると端末を特定できる。`app.js` は**採用リストを通した後の値しか変数に持たない**（生の戻りを握った変数を作らなければ、そこから表示・保存する経路もそもそも作れない）。整形側でももう一度通すので、呼び出し側が忘れても外へは出ない。(3) **統計はローパス前の生入力から取る。** FIR とダウンサンプルの経路には触っていない（直前のコミットで折り返し歪みを潰した箇所）。**この「順番」は目で確かめても守れない** — 後から `filter()` の後ろへ動かせば静かに壊れるので、`tests/audio-stats.test.ts` が**阻止域の 12kHz を入れて RMS が落ちないこと**で判定する。(4) **AudioWorklet は2行のスタブで Node から走らせられると分かった** — `AudioWorkletProcessor` / `registerProcessor` を `globalThis` に置いてから `import()` すれば `process()` は素の JS として叩ける。ESM のモジュールキャッシュがあるので import は1度きりにし、掴んだコンストラクタを `new` する。おかげで worklet 側は `app-wiring.test.ts` のようなソース文字列テストに頼らずに済んだ。(5) **`postMessage` の判別が配線の穴だった** — 統計を音声と同じポートで送るので、`e.data instanceof ArrayBuffer` で分岐しないと**統計オブジェクトがそのまま Deepgram へ送られる**。変異で確認済み。(6) **診断は独立した3つ目のエクスポート**にした。実機比較の結果を共有するときに会話本文を含むファイルを渡さずに済む。ただし**未保存警告の対象にはしていない** — 対象にすると診断を見ていない大多数のセッションでも毎回警告が出て、本当に守りたい文字起こし・用語カードの警告まで無視されるようになる（設計に指定が無かったので判断した点）。(7) **モードは `localStorage` に保存するのでホーム画面に常時表示する**。設定画面を開かないと分からない状態だと、前回スピーカー収音で使った設定のまま対面会議を始める。保存値は信頼境界の外なので `normalizeCaptureMode()` で丸める（素の `CAPTURE_MODES[mode]` だと `"constructor"` が truthy になり `constraints` が undefined のまま `getUserMedia` へ渡る。設計に無かったが `Object.hasOwn` で塞いだ）。(8) **word confidence は入れていない**（案C として先送り）。`protocol.ts` の変更を伴い、この Issue の主目的より一段広い話になるため。(9) 閾値 `CLIP_THRESHOLD` / `SILENCE_THRESHOLD` は**暫定値**で、説明は `diagnostics.js` の1箇所だけに置いた（写すと決めたときに片方を直し漏れる）。(10) 変異は6系統すべて検出を確認。**1件だけ等価変異**があった — `trackSettingRows()` の反復を `TRACK_KEYS` から `Object.keys(picked)` に変えても、`pickTrackSettings()` が正しければ挙動が変わらない（挿入順が一致する）。多層防御としては `TRACK_KEYS` を回す方が強いのでコメント付きで残した。(11) 人の判断待ちは3つ（スピーカーモードの constraints の妥当性、閾値、Pixel 9a での2モード比較）。[[termlens-open-issues]] に記録した。

2026-08-28 update — Issue #26 のレビュー(コード/セキュリティ + simplify 4観点)に対応。**#25 とまったく同じ形の指摘が出た**: 閾値というスカラーで解いていた問題が、実は機構の不足だった。(1) **閾値ではなく分布を出す** — `CLIP_THRESHOLD` / `SILENCE_THRESHOLD` で数えた比率だけを出していたが、**閾値を決めるために要る分布を閾値自身が壊していた**(無音率 100% と出ても実際のノイズ底が -30dBFS か -60dBFS か分からない)。人タスク「実機の数値を見て閾値を決める」が実行不能だった。機構が足りていない印は差分の中にあり、レビュー対応で足した `peak` の理由が「クリップ率 0% のときピークが分からないと閾値の妥当性を判定できない」＝**コード自身が「このスカラーは背後の分布なしでは読めない」と書いていた**。worklet が窓ごとの RMS を dBFS ビン(16本)に積み、水準は**表示時に当てる読み値**にした。クリップ側は分布にしない(端末に依らない量で、5dB 幅では 0dBFS 直下の解像度が足りない)。(2) **無音判定の窓を時間で定義** — render quantum 単位は分母が「`process()` が呼ばれた回数」で、ブラウザの実装粒度に依存する。閾値は変えられるのに窓長だけホストのブロック長に溶接されている非対称は層として逆。100ms 窓にしたら「無音率(ブロック)」「クリップ率(サンプル)」という単位の注記も要らなくなった。(3) **postMessage をタグ付き封筒に** — `instanceof ArrayBuffer` の型判別は「それ以外は全部統計」で**判別の空間に3つ目の席が無い**。3種類目を送ると例外にならず統計に畳み込まれ、`num()` が 0 に潰して静かに壊れる。`{type}` で分岐し `default` で捨てる形に。(4) **診断の状態4変数を1本に** — 同じライフタイムを持つのに別々の変数だと「全部そろっているか」が代入順とコメントで守られる。`diag = null` の1行で初期化できるようになり、テストも4本の文字列列挙から1本に縮んだ。(5) **レビューが実バグを1件検出** — 開始に失敗しても `sessionCaptureMode` が残り `hasDiagnostics()` の意味が壊れていた(マイクが開けてから一度に作る形で解消)。(6) 恒真テストを1本発見 — スタブ側が `instanceof` で振り分けていたので、その後で型を確認するテストは**実装を壊しても落ちなかった**。スタブは封筒をそのまま積み、分類はテスト側に移した。そのほか CSS の重複、`el()` の使い忘れ、含意で済むアサーション6件、閉じた `<details>` を毎秒描き直していた件(Efficiency の実測で唯一「直す価値がある」とされた項目)に対応。**Efficiency は他を全部「測った結果、無視できる」と結論** — `collectStats()` は音声スレッド予算の 0.0094%、FIR への統合で浮くのは 0.0022%、`Math.sqrt` の除去は逆に遅くなる(V8 が単一命令にインライン化)。変異6系統で検出を確認。

2026-08-29 update — Issue #36「話者ラベルの揺れ（speaker jitter）で 1 発話が細切れに表示される」に対応。実機の測定値（約49分・複数話者で 642 グループ中 3 文字以下が 255 件＝約39.7%、その約78%が同一秒内に複数 speaker が切り替わる箇所）を根拠に、**表示・エクスポート用の後処理**として補正を入れた。中心の判断は **「何も削除しない」** — 直すのは `speaker` ラベルだけで、テキストも行数も減らない。jitter と判定した行は前後と同じ話者になり同じ段落に入るので、AC「短い相槌を無条件に削除しない」を閾値の当たり外れではなく構造として満たす。`splitBySpeaker()` と raw の `finalLines` は無変更で、`localStorage` に保存されるのも用語抽出が見るのも補正前のデータのまま。判定は「前後が同じ確定話者の短い島」かつ「3 件が同じ `seq`（= 同じ final 由来）」で、別 final の相槌は長さに関わらず吸収されない。`seq` を欠く行（#36 以前の保存セッション）だけ受信時刻の窓へフォールバックする。`groupUtterances()` は `public/app.js` から **`public/utterances.js`** へ移し（`card-status.js` / `terms-markdown.js` と同じ「Node のテストから読めるようにする」パターン）、閾値 2 つ（`JITTER_CHAR_LIMIT` = 4 / `JITTER_WINDOW_MS` = 500）をファイル先頭に集約した（当初は `JITTER_MAX_PASSES` も置いていたが、1 パスと結果が変わらない=動かしても何も起きない定数だったのでレビューで削除した）。同じ final 由来かの判定材料として `ServerMessage.transcript` に `finalSeq` を 1 つだけ追加。**採番は `session.ts` が持つ** — `buildFinalEvents()` は純関数なのでグローバルカウンタを持たせるとテストの決定性が壊れるため、分割の事実（`segIndex`、サーバー内部用。分割数は読み手がいないので載せない）だけをイベントに載せ、`segIndex === 0` でカウンタを進める形にした。`segIndex` は**空の text を捨てた後に**振る（捨てる前に振ると先頭が落ちたとき採番が進まず、別々の final が同じ番号になって結合されうる）。テストは `tests/utterances.test.ts` と `tests/fixtures/speaker-jitter.json`（合成データのみ。話者・長さ・`seq` の組み合わせは fixture へ集約し集合一致で固定。片側だけの `seq` 一致で吸収しないこと、補正済みの結果を次の判定に使うことを識別するケースを含む。補正の前後でテキストが 1 文字も欠けないこと、入力を書き換えないことを明示的に固定）、`tests/app-wiring.test.ts`（**画面とエクスポートが同じ `groupUtterances(finalLines)` を通ること**。片方が自前でまとめ直しても純関数のテストは全部緑のまま落ちない）、`session-wiring` と `split-by-speaker` に採番規則を追加。stt-pipeline / architecture / testing / open-issues の 4 ページを更新。**実機での前後比較と閾値の妥当性は未検証**として open-issues に残した。

2026-08-30 update — Issue #38「`TermCard` に不変の `cardId` を導入し term 依存の識別を分離する」。**挙動は1つも変えないリファクタ**で、次段階（term の後編集・`unresolved` の再評価）の土台だけを作った。採用は案A（サーバーがセッション内通番で採番し、クライアントは受信 ID をローカル ID へ写像する）。(1) `protocol.ts` — `TermCard.cardId` を追加し、`card_update` から `term` を落として `cardId` に置き換えた。`TermStatus` の JSDoc も cardId ベースに書き換えたが、**「`unresolved` から上へは戻さない」という方針自体は #24 のまま維持**（理由を「`card_update` が term で突き合わせるから」→「**この Issue では改名の経路を作らないから**」に更新）。(2) `scheduler.ts` — `nextCardId` で `c1`, `c2`, … を配る。**採番は速報を組み立てる前に配り切り**、`toClientCard()` と `enrichCard()` の両方へ同じ ID を渡す（`toClientCard()` の中で採ると清書側に同じ ID を渡せず、更新がどのカードにも当たらない）。`onCardUpdate` の3経路（裏付けあり / 棄却 / 例外時のフォールバック）すべてで同じ ID を使う。**`selectVerifyTargets()` は term ベースのまま**にした — あれは「どのカードを検証に回すか」の選定であって識別ではなく、評価ハーネス（`src/eval/run.ts`）と共用の純関数なので、cardId を持ち込むと本番と評価が drift する。ログ（`console.warn` / `console.error`）は人が読むものなので従来どおり term を出す。(3) `public/app.js` — Map を **3本**に分けた（`cardData`: localCardId → TermCard ＝ 識別、`termToCardId`: term → localCardId ＝ 意味上の同一性、`incomingCardId`: serverCardId → localCardId ＝ 受信 ID の写像）。**この分離が Issue の目的そのもの**なので片方へ寄せない。ローカル ID は `k1`, `k2`, …（サーバーの `c\d+` と見分けが付く形）。`dataset.term` を全廃して `dataset.cardId` にし、DOM 検索は **`findCardEl()` 1箇所に集約**した（各所で `[...cardsEl.children].find(...)` を書くと識別子を変えるたびに置換漏れが出る）。`activeTerm` → `activeCardId`、`pinnedToTerm` → `pinnedToCard`、`highlightOwner` の値もローカル ID にし、持ち主は **`addCard()` の戻り値**から取る（`card.cardId` を渡すと、再送で写像だけ貼り替えた場合に実在しないカードを指す）。**危ないのは2箇所だった** — (R1) 再接続でサーバーの採番が `c1` から振り直されるので、既知 term の再送でも `incomingCardId` を**畳み込みの可否に関わらず**貼り替える（`shouldApplyResend()` の分岐の中に入れると清書済みカードだけ写像が古いまま残る）。(R2) `shownTerms` を `[...cardData.keys()]` のまま放置するとローカル ID がサーバーの `normalizeTerm()` を通って `shownSet` に積まれ、**デデュープが丸ごと無効化される**（再接続後に既出カードが全部再表示される＝ #8 の回帰）ので `[...cardData.values()].map((c) => c.term)` に変えた。`termToCardId` のキーは**生の term 文字列**で正規化を挟まない（挟むとデデュープの当たり方が変わる）。保存/復元はスナップショットの `cardId`（＝保存時点のローカル ID）が `k\d+` ならそのまま採用し、**採番カウンタを追い越させる**（追い越さないと復元後の新カードが ID を衝突させる）。cardId の無い #38 以前の保存データは新しく採番するので、そのまま復元できる。`clearSessionContent()` は3本ともクリアし `nextLocalCardId` を 0 に戻す。(4) **`public/card-status.js` の判断ロジックは1行も変えていない**（`mergeCardUpdate` / `shouldApplyResend` / `cardStatus` / `cardHeading`）。`terms-markdown.js` も無変更で、`cardId` はエクスポートに出さない。(5) テストは既存の `card_update` の形を cardId へ直したうえで、`scheduler-verify`（採番の一意性・チャンクをまたぐ通番・更新と速報の ID 一致・例外パスも同じ ID）、`session-wiring`（`cardId` をそのまま中継し `term` を載せない。scheduler へ渡した配線を掴んで直接叩く）、`card-status`（`mergeCardUpdate()` が `cardId` を書き換えない）、`terms-markdown`（`cardId` が出ない）、`app-wiring`（3本の Map、term ベースのデデュープ、R1 の貼り替えが**分岐より前**にあること、R2、`updateCard` が term を見ないこと、`findCardEl` への集約、復元時の cardId 維持と旧データの採番、初期化で3本クリア）を追加した。app.js は Node から import できないので、クライアント側は既存の慣習どおり**ソース文字列テスト**で配線の形を固定している。**変異は20系統すべて検出を確認**（採番を定数化 / 清書側で別採番 / session が term を中継 / `shownTerms` にキーを送る / 写像の貼り替えを分岐の中へ / `updateCard` を term ベースへ / 採番カウンタを追い越さない / `addCard` が ID を返さない / `dataset.term` へ戻す / `findCardEl` を使わず直接検索 / 3本目の Map をクリアしない / Markdown に cardId を出す / `mergeCardUpdate` が update を素通し / デデュープを cardId ベースへ / `termToCardId` を登録しない / 復元でハイライトに term を渡す）。**うち4系統はレビュー指摘を受けて後から足したもの**— 初回の16系統では (a) 新規カードの写像登録を消しても、(b) `findCardEl()` に `card.term` を渡しても、(c) `adoptLocalCardId()` の衝突チェックが無くても、(d) 再送分岐の戻り値を捨てても、**450件全部が緑のまま**だった。特に (a) は「検証に回った全カードの `card_update` が1件も届かない」のに画面上は『確認中』が回り続けるだけで、例外もテスト失敗も出ない穴だった。写像の登録を `registerIncoming()` に括り出したのは、**新規と再送で2回呼ばれることを数えられるようにするため**。architecture に**プロトコル表**を新設し、term-extraction に「カードの識別子 `cardId`」節、testing に上記の回帰テストの意図、`terms-markdown.test.ts` の行（表から抜けていた）を追加した。
2026-09-01 update — Issue #40「後続文脈で unresolved 用語カードを再評価して確定候補へ更新する」。案A（後続の確定カードをトリガに既存 Stage 2 で再検証）で実装した。(1) `ExtractionScheduler` に `pendingUnresolved` を持たせた。`{cardId, term, surfaceForms, correctedFrom, candidates, 文脈抜粋, attempts, lastAttemptAt}` を`MAX_PENDING_UNRESOLVED`(20) 件まで、超えたら古いほうから捨てる。**`candidates` はこれまで `enrichCard()` に渡した後で捨てていた**ので、保持経路を1本足した。(2) 新規モジュール `src/extract/rematch.ts`（`toKana` / `similarity` / `isRelated` / `mergeCandidates` / `isResolved` / `pickCandidate`）。**依存ゼロで保つ** — `normalize.ts` / `glossary.ts` と同じ理由で、純関数のテストが `new OpenAI()` を連れ込まないようにする。値として取るのは `schema.js` の `MAX_CANDIDATES` だけ（zod しか読まない）。(3) トリガは `run()` で `status !== "unresolved"` のカードが出たとき。**pending へ積むより先にトリガを引く** — 同じチャンクで出た unresolved を同じチャンクの確定カードで再評価しても、抽出段が両方を見たうえで unresolved にしている以上新しい材料が無く、順序を逆にすると unresolved が出るたびに web 検索つきの呼び出しが1本増える。(4) 再検証は **`verifyAndEnrich()` をそのまま再利用**した。関数の口が絞り込み済みの `glossaryHints` しか受けないので、#25 のプライバシー原則が型のまま継承される（渡す文脈は「unresolved が出たチャンクの抜粋 + 直近の `ContextWindow`」で、会話全文ではない）。(5) **`isVerified()` は1文字も触っていない。** あちらは「表示中の term が裏付けられたか」で、候補#2 が選ばれたら false を返すのが #24 の降格判断そのもの。一律に緩めると通常の Stage 2 の降格が壊れるので、昇格の判定は新しい述語 `isResolved(chosen, candidates)` に分けた。候補外の遮断は `parseVerifyOutput()` と合わせて**2枚重ね**になる。(6) `protocol.ts` の `card_update` に `rename?: { term, reading, correctedFrom, surfaceForms }` を追加。**入れ子にしたのは「フラグは立っているが term が無い」矛盾した組を型として書けなくするため。** `cardId` は入れない（改名で ID が変わる経路を型が許してしまう）。**既存の3経路（裏付けあり / 棄却 / 例外フォールバック）は付けない**ので挙動は #24 のまま。`session.ts` は無いときキーごと落として中継する。(7) コスト制御は5段 — `REMATCH_MIN_SIMILARITY`(0.5) / `MAX_REMATCH_ATTEMPTS`(2) / `REMATCH_COOLDOWN_MS`(30s) / `MAX_REMATCH_PER_RUN`(2) / `MAX_PENDING_UNRESOLVED`(20)、加えて `verifyDisabled`。前3つは env ノブ（`config.ts` に `ratioEnv()` を新設。閾値だけは小数でないと意味が無く、範囲外は起動時に落とす）。**`REMATCH_MIN_SIMILARITY=0` は素通し**で、`MAX_WEB_SEARCHES=0` と同じ「分布を測ってから値を決める」約束。**試行は投げた時点で数える** — 結果を待つと、検証が遅い間に次のチャンクが来て同じカードを何本も投げられる。(8) 昇格に成功したら pending から外し、確定した用語を `shownTerms` に載せる（#24 が枠を空けていた理由がそこで消える。載せないと後続チャンクで同じ用語のカードがもう1枚出る）。**失敗しても `card_update` は送らない** — 再評価の対象は `willEnrich` を予告していない既存カードなので、送っても意味のない更新が増えるだけ。(9) クライアントは `mergeCardUpdate()` に `rename` の分岐だけを足した（**`shouldApplyResend()` は変更なし**。速報の再送に `rename` は無いので #24 のガードがそのまま正しい）。統合ルールは純関数 `mergeDuplicateCards(keep, drop)` を `card-status.js` に新設し、DOM 反映は `app.js` の `mergeRenamedCard()` に分けた。残す cardId は**登場順が早いほう**、`surfaceForms` は 残す側 → 消す側 の順で連結、`links` は残す側優先で3件まで補完。`incomingCardId` と `highlightOwner` は**全件走査で張り替える**（`reassignIncomingCardId()` / `reassignHighlightOwner()` を新設。同じカードを指す serverCardId は再接続のたびに増えるので、1件だけ直しても例外は出ず更新が迷子になる）。`activeCardId` が消える側を指していたら残す側へ移す。**古い surface form のハイライトは残す** — 文字起こし本文は崩れた表記のまま残る（この Issue は raw transcript を書き換えない）ので、消すと過去の行からカードへ辿れなくなる。(10) 評価ハーネスに `rematchAttempts` / `rematchPromoted` / `rematchCorrect` / `rematchMiscorrected`（`CaseScore`）と `rematchPromotion` / `rematchMiscorrection`（`Metrics`）、`TermCase.expectRematch` を追加し、`formatTable` に「再評価」行を新設した。**昇格率の分母 0 は 0 に倒す** — `ratio()` の「分母 0 は減点しない＝1」だと、機能が一度も発火していないランが「昇格率 100%」に見える。ハーネスも本番と同じ述語で再評価を回すが、**1ケース＝1チャンクなのでトリガの時間差だけは作れない**。測れるのは判断の質で、発火頻度は実機でしか分からない。(11) テスト: `rematch.test.ts`（純関数28本）/ `scheduler-rematch.test.ts`（昇格・維持・無関係・上限4種・cooldown・verifyDisabled・プライバシー、16本。**時計を差し替えて** cooldown を待たずに跨ぐ）/ `merge-duplicate.test.ts`（統合ルール12本）を新設し、`card-status`（**rename 無しでは昇格させない #24 の回帰**を含む5本）/ `app-wiring`（改名と統合の配線6本。**`finalLines` に触らない**ことも悪い形の不在で固定）/ `session-wiring`（rename の透過2本）/ `terms-markdown`（改名後の内容2本）/ `eval-metrics`（再評価指標7本）/ `config-env`（新ノブ3本）に追加。fixture に `rematch-garbled-then-clear` を1件足した（**匿名化した合成データのみ**。既存の `unresolved-*` 2件は `expectRematch` を空のままにしてある — あの2件は「断定しないのが正解」なので、昇格したら必ず誤補正に数えられるのが正しい）。532件（うち LLM 評価1件は skip）が緑。**変異は4系統で検出を確認**（1 run あたりの上限を外す / 昇格後に pending から外さない / `mergeCardUpdate` の unresolved ガードを外す / `mergeDuplicateCards` の残す側優先と `MAX_LINKS` を外す）。`isResolved()` を `chosen !== null` に緩める変異は**検出されなかった** — `parseVerifyOutput()` が先に候補外を弾くため、スケジューラ経由では観測できない多層防御になっている（述語そのものは `rematch.test.ts` が固定）。**上限値・閾値は5つとも暫定で、1つも計測していない。** 決め方（`REMATCH_MIN_SIMILARITY=0` で分布を測る手順）と、人がやる必要のあること4項目を open-issues に明記した。
